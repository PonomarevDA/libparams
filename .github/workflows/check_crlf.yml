name: check_crlf

on: [push]
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
        with:
          path: 'repository'

      - run: sudo apt-get install dos2unix
      - run: repository/scripts/check_crlf.sh
